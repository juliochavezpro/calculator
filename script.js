const buttons = document.getElementById('.buttons');

const playingPress = buttons.addEventListener('keydown', function(e) {

});

const playingClick = buttons.addEventListener('click', function(e) {
    buttons
});